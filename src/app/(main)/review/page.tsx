import { RecipeReviewForm } from "@/components/voice/RecipeReviewForm";

export const metadata = { title: "Review Recipe — RecipeVerse" };

export default function ReviewPage() {
  return <RecipeReviewForm />;
}
