import { VoiceRecorder } from "@/components/voice/VoiceRecorder";

export const metadata = { title: "Voice Recipe — RecipeVerse" };

export default function VoicePage() {
  return <VoiceRecorder />;
}
